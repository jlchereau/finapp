"""A data provider for BlackRock data."""

import httpx
from pandas import DataFrame
from pydantic import BaseModel
from .base import (
    BaseProvider,
    ProviderType,
    ProviderConfig,
    NonRetriableProviderException,
    RetriableProviderException,
)
from .cache import cache
from .parsers import PydanticJSONParser, ParserConfig


class BlackrockDataProvider(BaseProvider[DataFrame]):
    """
    A data provider for BlackRock data.

    This class extends BaseProvider to implement specific methods
    for fetching and caching BlackRock data.
    """
